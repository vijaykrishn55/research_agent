# Research-Agent Dependencies
# Core API
fastapi==0.115.12
uvicorn==0.34.3
python-multipart==0.0.20
python-dotenv==1.1.0

# LLM Providers
groq==0.25.0
openai==1.82.0

# Embeddings & Vector Search
sentence-transformers==4.1.0
faiss-cpu==1.11.0

# Document Processing
pypdf==5.6.0

# Utilities
numpy>=1.26.0
pydantic>=2.0.0

# Web Search (optional — only needed for web/hybrid/auto modes)
tavily-python>=0.5.0

# Export (optional — only needed for --export md/pdf)
markdown>=3.5
xhtml2pdf>=0.2.11        # Pure-Python PDF renderer (no GTK needed on Windows)
